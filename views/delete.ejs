<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Student Confirmation</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0b0c10; /* Dark background color */
            margin: 0;
            padding: 0;
            color: #c5c6c7; /* Light text color */
        }
    
        header {
            background: linear-gradient(to bottom, #1f2833, #292e32); /* Dark header background gradient */
            color: #66fcf1; /* Header text color */
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
        }
    
        h1 {
            margin: 0;
            font-size: 24px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
    
        .container {
            background-color: #1f2833; /* Dark container background color */
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
        }
    
        /* Add your other styles here */
    
        .confirmation-text {
            font-size: 18px;
            margin-bottom: 20px;
        }
    
        .delete-button {
            margin-right: 10px;
            background-color: #dc3545; /* Delete button color */
            color: #0b0c10; /* Text color on delete button */
            border: none;
        }
    
        .btn-secondary {
            background-color: #6c757d; /* Secondary button color */
            color: #0b0c10; /* Text color on secondary button */
            border: none;
        }
    
        .delete-button:hover {
            background-color: #c82333; /* Darker delete button color on hover */
        }
    
        .btn-secondary:hover {
            background-color: #545b62; /* Darker secondary button color on hover */
        }
    </style>
    
</head>
<body>
    <header>
        <h1>Delete Student</h1>
    </header>
    <div class="container">
        <div class="confirmation-text">
            Are you sure you want to delete this student?
        </div>
        <form action="/delete" method="post">
            <input type="hidden" id="id" name="id" value="<%= id %>">
            <button type="submit" class="btn btn-danger delete-button">Delete</button>
            <a href="/" class="btn btn-secondary">Cancel</a>
        </form>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
